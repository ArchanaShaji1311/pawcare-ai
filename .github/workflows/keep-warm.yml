name: Keep backend warm

on:
  schedule:
    - cron: "*/12 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health endpoint
        run: |
          curl -s -m 60 "https://pawcare-ai-archana-bht2.onrender.com/api/health" || true
